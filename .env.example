# === Django core ===
SECRET_KEY=change-this-to-a-random-long-string
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=
SITE_URL=http://localhost:8000

# === Database ===
# Leave blank to use local SQLite. For Postgres:
# DATABASE_URL=postgres://tapstar:tapstar@localhost:5432/tapstar
DATABASE_URL=

# === Redis / Celery ===
REDIS_URL=redis://localhost:6379/0
CELERY_TASK_ALWAYS_EAGER=True

# === Anthropic (Claude) ===
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# === Easebuzz (Indian payment gateway) ===
EASEBUZZ_KEY=
EASEBUZZ_SALT=
EASEBUZZ_ENV=test

# === Email ===
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=Tapstar <noreply@tapstar.in>

# === Media ===
# MEDIA_ROOT=/absolute/path/to/media
